import React from 'react';
import { FooterContainer } from './Footer.styles';

export const Footer = () => {
    return(
        <FooterContainer>
            USERS APP
        </FooterContainer>
    )
};